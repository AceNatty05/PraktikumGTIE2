{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build and Run GLUT",
      "type": "shell",
      "command": "g++ ${file} -o ${fileDirname}/app -I/opt/homebrew/include -L/opt/homebrew/lib -lglut -framework OpenGL && ${fileDirname}/app",
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": []
    }
  ]
}